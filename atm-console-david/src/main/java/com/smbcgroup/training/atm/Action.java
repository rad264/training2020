package com.smbcgroup.training.atm;

public enum Action {
	login, changeAccount, checkBalance, deposit, withdraw, addAccount, showAccounts, transfer, transfer2, transferAmt, showHistory;
	// TODO: add more actions
}